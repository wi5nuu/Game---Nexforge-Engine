[package]
name = "nexforge-net"
version = "0.1.0"
edition = "2021"
description = "Rollback netcode with GGPO-inspired deterministic simulation"

[dependencies]
thiserror = { workspace = true }
bincode = { workspace = true }
serde = { workspace = true }
bytemuck = "1"
