[package]
name = "nexforge-profiler"
version = "0.1.0"
edition = "2021"
description = "Frame profiler (F1) with GPU timing and memory tracking for Nexforge Engine"

[dependencies]
serde = { workspace = true }
serde_json = "1"
thiserror = { workspace = true }
